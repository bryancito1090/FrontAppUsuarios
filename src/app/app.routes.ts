import { Routes } from '@angular/router';
import { LoginComponent } from './login/login.component'; // Asegúrate de ajustar la ruta de importación según la estructura de tu proyecto

export const routes: Routes = [
  { path: '', component: LoginComponent }
];